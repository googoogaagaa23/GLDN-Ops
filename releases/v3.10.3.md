# GLDN Ops v3.10.3

## Scope

- Set the correct Move .99 defaults for Computer `2` / `FANCYFI`.
- Repair the M0-reported reverse Move Non-.99 category-discovery failure without changing marketplace approval boundaries.

## Computer 2 Defaults

- Forward source Store categories: `SNI`, `SNIPO v2`
- Forward destination Store category: `DAILY`
- Reverse source Store category: `DAILY`
- Reverse destination Store category: `SNI` (the first configured non-sale source)

Existing Computer `2` installations still carrying the old generic defaults migrate automatically. Explicit custom settings remain unchanged.

## Reverse Repair

Reverse mode previously learned the numeric ID for the sale category and could save the reversed category pair over the account's forward configuration. The user encountered the broken behavior on M0 / CLICKNCARRY, whose reverse direction is `BALK -> BEST SELLERS`. The discovered ID is now scoped to the current reverse run for every account. It remains available for exact scanning and final verification without altering the saved forward source categories, destination, or source IDs.

## Safety

- The workflow still performs a complete exact-ID scan before creating an edit workspace.
- Invalid or missing prices and backburner items remain excluded.
- No listing is submitted automatically.
- Every eBay Submit remains a separate action-time approval stop.

## Verification

- Focused Computer `2`, migration, Move .99, and reverse contracts pass `62/62`.
- The complete automated JavaScript suite passes `231/231`.
- M0 / CLICKNCARRY signed-in `BALK -> BEST SELLERS` scan and final-review proof is still required before reverse cleanup is called live-ready.
