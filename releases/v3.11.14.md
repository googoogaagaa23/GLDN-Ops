# GLDN Ops v3.11.14

## Scope

- Make live host-theme verification dependable when browser screenshots are unavailable.
- Make stale workflow recovery available through the existing Profile-2-only local controller.
- Preserve every marketplace and dashboard approval boundary.

## Diagnostics And Recovery

- The bounded page inspector now reports computed background, text, color-scheme, and opacity values for the host roots and visible GLDN-owned panels or modals.
- The inspector reports `gldnThemeLeak=true` only when legacy generic GLDN theme settings or short CSS aliases remain on the marketplace root. Namespaced GLDN markers are expected and are not leaks.
- `ResetState` uses the existing automation reset implementation, clears shared workflow checkpoints, closes the background profit worker, and broadcasts best-effort UI cleanup.
- `ReloadExtension` records an auditable reload request and reloads the already-installed Profile 2 runtime after returning its controller acknowledgement; it does not start Chrome or open a new tab.
- The eBay `show-panel` inspection action reveals only the GLDN panel and starts no marketplace workflow.
- The command remains protected by the random per-install token, localhost-only transport, exact loaded-folder check, and Profile 2 lock.

## Safety

- The new inspector does not return marketplace body text, listings, messages, addresses, or credentials.
- The reset command cannot click a marketplace control or finalize any listing, order, note, dashboard, spreadsheet, or purchase action.
- eBay Submit, Save, Apply, Continue, Mark as shipped activation, and all purchase controls remain outside the local-control allowlist.

## Verification

- Local-control and host-theme contracts pass `9 / 9`.
- The complete JavaScript suite passes `267 / 267`.
- Signed-in Profile 2 bounded appearance readback passed on eBay Messages and Poshmark My Posh Stats: both hosts returned `gldnThemeLeak=false`, no generic theme/color-scheme settings, and zero leaked aliases while GLDN panels and the Poshmark review retained dark translucent surfaces.
- `Save Poshmark Stats` was untouched. The token-protected reset returned the runtime to zero active workflows and zero open reviews.
- Evidence: `evidence/profile2-u01-theme-v31114-2026-07-27/U-01-v31114-live-appearance.json`.
- U-01 remains short of full `LIVE PASS` until the required local MP4 and verified Drive link are recorded.
