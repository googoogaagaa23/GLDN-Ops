# GLDN Ops v3.12.32 - Reliability Candidate

## Changes

- Cancellable review registration and ordered cleanup; unrelated approvals remain blocking.
- Explicitly paired control identities work with shared multi-profile extension folders. No webpage heartbeats; unavailable agents back off. State-changing commands are never automatically redispatched after a lost response.
- Health & Installations shows runtime, identity, updater/file/release versions, saved progress, review owners, queued records, and recent issues.
- Plan-first Chrome policy setup preserves unrelated entries. The separate complete Web Store package uses Chrome-managed updates.
- The local installer now includes all three workflow-guide files and rejects any future public runtime omission.

## Upgrade Boundary

Extension-only updates repair reviews and remove webpage polling. New pairing and inventory require the matching Windows helper. Until stable promotion, the generic Setup.exe still downloads the prior stable release; use the versioned candidate package only for supervised validation. No unpacked extension is silently installed in additional profiles. Existing credentials, settings, marketplace rows, spreadsheet rows, and checkpoints are not automatically migrated, cleared, or submitted. Pairing credentials are not included in settings backups.

## Verification

499 JavaScript tests, PowerShell pairing/policy fixtures, and actual isolated Chrome UI runs passed for both deployment channels. The Store build's narrower host scope and initially ungranted helper permission were checked. Current signed-in marketplace validation, settings-preserving deployment, and Web Store submission/review remain pending. Automated and isolated-browser checks are not a live marketplace pass. No current-version Drive live proof exists yet.
