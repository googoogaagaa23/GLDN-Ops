# GLDN Ops v3.12.44

## Fixed

Connect Dashboard could fail with "U is not defined" before opening a setup-code prompt. It now opens a masked input directly inside the popup. Paste the existing code and choose Save & Test.

Cancel does not change saved settings. Invalid input is rejected before saving, connection-test failures remain visible, and setup codes stay out of public packages and error feedback.

## Update

On Computer 6, choose Update & Reload. Open Status > Connect Dashboard, paste the existing private setup code, and choose Save & Test. After "Dashboard connected securely", retry the policy scan or shared-history sync.

No listing changes are performed. This release does not change the one-workflow-at-a-time lock.

## Verification

- Full release checks passed: 602 automated tests, installer/update fixtures and package safety checks.
- Actual popup files passed isolated Chrome checks at 420px and 390px widths, with no page errors: masked entry, Save & Test, cancellation, and visible failed-connection feedback.
- Executable handler tests also cover invalid input, Enter/Escape, duplicate clicks, storage errors and timeouts. All test credentials are fictitious.

Computer 6 is remote: its installed version and actual dashboard connection must be verified there after updating. No live marketplace changes are claimed.
